[package]
name = "model-merger-worker"
version = "2.0.1"
edition = "2024"
license = "MIT"
rust-version = "1.96"

[dependencies]
model-merger-engine = { path = "../model-merger-engine" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[[bin]]
name = "model-merger-worker"
path = "src/main.rs"
