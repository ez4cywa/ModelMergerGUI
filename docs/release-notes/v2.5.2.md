# Cast Model Merger GUI v2.5.2

## 仓库清理（无功能变化）

- 移除迁移期遗留代码 146 个文件、约 2.3 万行：历史 WPF GUI/CLI（`src/`）、PhilLibX 与 SELibDotNet 兼容对照、C# 测试项目、迁移工具与已停用的协议 worker。全部功能由 Rust 原生实现，不受影响。
- `tests/fixtures/rust-migration` 黄金语料保留，继续作为 Rust 合并与预览测试的对照基准。
- 更新 README 与用户指南的工程结构章节；`.gitignore` 清理失效条目并忽略本地会话目录。
- 本版本程序与 v2.5.1 功能一致，仅为仓库健康度清理。

Repository cleanup with no functional changes: 146 files / ~23k lines of migration-era code removed (legacy WPF GUI/CLI under `src/`, PhilLibX and SELibDotNet references, the C# test projects, the migration harness and the retired protocol worker). The `tests/fixtures/rust-migration` golden corpora stay as the baseline for the Rust merge and preview tests. README / user-guide structure sections were updated accordingly. The program is functionally identical to v2.5.1.

解压 ZIP 后运行 `CastModelMerger.exe`，无需安装 .NET 或 Rust。Extract the ZIP and run `CastModelMerger.exe`; no .NET or Rust installation required.
